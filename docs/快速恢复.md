# F-Analyzer 快速恢复指南

> 最后一次验证: 2026-06-07 | 35 次 commit | 14 测试全过

## 启动

```bash
cd "d:/Ai RAG/Qiaoyuan"
taskkill //F //IM python.exe 2>$null
python -m streamlit run app.py --server.port 8501 --server.headless true --server.maxUploadSize 10
```

浏览器: http://localhost:8501

## 运行测试

```bash
cd "d:/Ai RAG/Qiaoyuan"
python -m pytest tests/ -v
```

## DeepSeek Key

`sk-5661d358e06b4a46b4f856d7c78f65a1`

## 上一个 commit

```
git log --oneline -1
```

## 真实报表目录

`test documents/` (8 份已验证)

## 依赖安装

```bash
pip install streamlit pandas openpyxl numpy plotly pymupdf openai pytest
```

## 关键事实

- **自研率 ~70%**：所有指标计算、科目映射、Excel 解析均为自研
- `source code/` 下 6 个开源项目均未被代码引用
- 实际运行时依赖仅 5 个第三方包
