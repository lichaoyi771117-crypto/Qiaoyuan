# 峤远·AI财务报表分析 — 安全审计报告

> 审计时间：2026-07-14
> 审计范围：全量代码扫描

---

## 一、审计结论

| 级别 | 数量 | 备注 |
|------|------|------|
| 🔴 高危 | 0 | — |
| 🟡 中危 | 0 | — |
| 🟢 低危 | 0 | — |
| ✅ 亮点 | 3 | 临时文件清理、API Key 管理、LLM 调用防失败处理 |

---

## 二、检查详情

### ✅ 安全亮点

**1. 临时文件清理（`finally` 确保删除）**

**文件**：`app.py` 第 320 行

```python
with tempfile.NamedTemporaryFile(delete=False, suffix=ext) as tmp:
    tmp.write(cf["content"])
    tmp_path = tmp.name
try:
    sheets = pd.read_excel(tmp_path, ...)
finally:
    os.unlink(tmp_path)
```

使用 `try/finally` 结构，无论 `pd.read_excel` 是否抛异常临时文件都会被删除。写法正确。

**2. API Key 回退值为空字符串**

**文件**：`app.py` 第 29 行

```python
DEEPSEEK_API_KEY = os.getenv("DEEPSEEK_API_KEY", "")
```

注意：乔曦用的是 `os.environ.get("DEEPSEEK_API_KEY", "sk-your-key-here")`（含占位符），而峤远直接用空字符串，更安全——没设 Key 时 API 调用失败而不是用假 Key。

**3. LLM 调用优雅退化**

**文件**：`src/report_generator.py` 第 348-354 行

当 API Key 未配置时，报告生成不崩溃，而是降级为展示已有的指标计算值，并提示用户需要配置 API Key。这种设计在所有子程序中表现最好。

---

## 三、已通过的安全检查

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 命令注入 | ✅ | 无 exec/eval/subprocess |
| SQL 注入 | N/A | 未使用数据库 |
| XSS | ✅ | Streamlit UI 无用户输入直接注入 HTML |
| 文件上传后缀 | ✅ | `os.path.splitext(...)` 提取后缀 |
| 临时文件清理 | ✅ | try/finally 确保删除 |
| API Key 管理 | ✅ | 环境变量读取，回退空字符串 |
| 敏感信息泄露 | ✅ | 无日志泄露 PII |

---

## 四、结论

峤远的代码安全质量在 4 个子程序中与乔曦并列最高。**无任何漏洞需要修复。** 代码结构清晰、异常处理完善、API Key 管理正确，可以直接上线。

---

*本报告由 AI 辅助代码安全审查生成，非专业渗透测试。*
